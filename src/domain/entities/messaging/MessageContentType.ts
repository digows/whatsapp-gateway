/**
 * Supported content kinds handled by the gateway today.
 */
export enum MessageContentType {
  Text = 'text',
  Image = 'image',
  Audio = 'audio',
  Video = 'video',
  Document = 'document',
  Other = 'other',
}
